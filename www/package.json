{
  "name": "freeapp",
  "version": "1.0.0",
  "main": "marked.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vercel dev",
    "build": "node scripts/build-env-inject.js",
    "build:dev": "ENVIRONMENT=development node scripts/build-env-inject.js",
    "build:staging": "ENVIRONMENT=staging node scripts/build-env-inject.js",
    "build:prod": "ENVIRONMENT=production node scripts/build-env-inject.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "pg": "^8.11.0",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
